{
  "name": "sorting_design_pattern",
  "version": "1.0.0",
  "description": "in this project we will not use parcel but we gonna use node directly  #1- create a build directly for generated files #2- create a src directory for sources files  #3- run tsc --init to generate a tsconfig.json file to customize the compiler  ##3-1 set rootDir to src ##3-1 set outDir  to build  #now running \"tsc\" will compile all the files from  the rootDir and ut them  in the outDir   #running \"tsc -w\": watch flag allows to automatically recompile all the code in case we make some changes",
  
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:build" : "tsc -w",
    "start:run": "nodemon build/index.js",
    "start": "concurrently npm:start:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.0.0",
    "nodemon": "^2.0.7"
  }
}
